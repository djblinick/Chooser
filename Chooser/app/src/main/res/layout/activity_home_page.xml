<!-- Home page of all the dilemmas. -->
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="#fff9fffb"
    tools:context="com.biu.ap2.winder.chooser.HomePageActivity">

    <!-- Top text. -->
    <TextView
        android:id="@+id/homepagetitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerInParent="true"
        android:layout_marginTop="10dp"
        android:text="@string/home_page_title"
        android:textAllCaps="true"
        android:textSize="40dp"
        android:textColor="#fa000000"
        android:textAlignment="center"
        android:background="#fff9fffb"/>

    <!-- Add a new dilemma. -->
    <ImageButton
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_below="@+id/homepagetitle"
        android:layout_alignRight="@+id/homepagetitle"
        android:layout_alignEnd="@+id/homepagetitle"
        android:layout_marginTop="15dp"
        android:src="@drawable/addbutton"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:id="@+id/add_decision_button"
        android:layout_gravity="end"
        android:background="#fff9fffb"/>

    <!-- Delete memory. -->
    <ImageButton
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:scaleType="fitCenter"
        android:src="@drawable/delete"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:id="@+id/deleteMemory"
        android:text="delete memory"
        android:background="#fff9fffb"/>

    <!-- List of dilemmas. -->
    <FrameLayout
        android:id="@+id/DecisionsFragment"
        android:layout_above="@+id/deleteMemory"
        android:layout_below="@+id/add_decision_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
    </FrameLayout>

</RelativeLayout>
