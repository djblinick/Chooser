<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:id="@+id/helper_activity_layout"
    tools:context="com.biu.ap2.winder.chooser.HelperActivity">

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:id="@+id/tracker">

        <TextView
            android:text="helper activity"
            android:layout_width="wrap_content"
            android:layout_gravity="center"
            android:id="@+id/helper_name"
            android:layout_height="wrap_content" />

        <ListView
            android:id="@+id/lstSteps"
            android:scrollbarAlwaysDrawHorizontalTrack="true"
            android:divider="@null"
            android:layout_height="wrap_content"
            android:layout_width="match_parent">
        </ListView>

    </LinearLayout>

    <TextView
        android:text="helper activity"
        android:layout_width="wrap_content"
        android:layout_gravity="center"
        android:id="@+id/fragment_title"
        android:layout_below="@+id/tracker"
        android:layout_height="wrap_content" />

    <FrameLayout
        android:id="@+id/HelperFragment"
        android:layout_below="@+id/fragment_title"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent">
    </FrameLayout>

</LinearLayout>
